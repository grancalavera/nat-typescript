{
  "name": "nat-typescript",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "typescript": "^3.5.1"
  },
  "devDependencies": {
    "@types/node": "^12.0.3",
    "nodemon": "^1.19.1",
    "ts-node": "^8.2.0"
  },
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/main.ts",
    "build": "tsc -p ."
  }
}
